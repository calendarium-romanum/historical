RSpec.describe Calendarium::Romanum::Historical do
  it "has a version number" do
    expect(Calendarium::Romanum::Historical::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
